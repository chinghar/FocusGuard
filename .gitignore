.venv/
__pycache__/
*.pyc
.DS_Store

# Local runtime data — config/db live in ~/.focusguard, never in-repo,
# but ignore just in case a dev points the path here for testing.
*.sqlite3
/focusguard_data/
